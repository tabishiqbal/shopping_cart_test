<div class="page-header">
  <h1>Cart No. <%= issuance.id %></h1>
</div>

<div class="table-responsive">
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Tool Barcode</th>
        <th>Description</th>
      </tr>
    </thead>

    <tbody>
      <%= render(issuance.line_items) %>
    </tbody>
  
  </table>
</div>
<%= button_to 'Discard Issuance', @issuance, method: :delete, data: {confirm: 'Are you sure?'} %>
<%#= link_to 'Edit', edit_issuance_path(@issuance) %> |
<%#= link_to 'Back', issuances_path %>
