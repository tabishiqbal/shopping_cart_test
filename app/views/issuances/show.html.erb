<% if notice %>
  <p><%= notice %></p>
<% end %>

<div class="table-responsive">
	<table class="table table-hover">
		<thead>
			<tr>
				<th>Tool Barcode</th>
				<th>Description</th>
			</tr>
		</thead>

		<tbody>
			<%= render @issuance %>
		</tbody>
	</table>
</div>

<%= button_to 'Discard Issuance', @issuance, method: :delete, data: {confirm: 'Are you sure?'} %>